# round_all

`def round_all(nums: list) -> list`

Given `nums`, a list of numbers, return a new list with each number **rounded to the
nearest integer**. The returned list has the same length and order as the input.
