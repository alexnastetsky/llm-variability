# camel_to_snake

`def camel_to_snake(s: str) -> str`

Convert a camelCase or PascalCase identifier `s` (letters only) to **snake_case**: all lowercase,
with words separated by single underscores. There are no leading, trailing, or doubled underscores,
and removing the underscores reproduces the original letters in lowercase.
