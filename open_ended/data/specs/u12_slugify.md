# slugify

`def slugify(s: str) -> str`

Convert the string `s` into a **URL slug**: a lowercase string with no spaces or punctuation,
suitable for use in a URL path. The slug must preserve the alphanumeric content of `s` in order.
