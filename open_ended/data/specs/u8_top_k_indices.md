# top_k_indices

`def top_k_indices(nums: list, k: int) -> list`

Given a list of numbers `nums` and an integer `k`, return the **indices of the `k` largest
values** in `nums`. If `k <= 0` return an empty list; if `k` exceeds the length, return indices
for all elements.
