# top_k

`def top_k(nums: list, k: int) -> list`

Given a list of numbers `nums` and an integer `k`, return the **`k` largest numbers**
from `nums`. If `k` is `0` or negative, return an empty list; if `k` is larger than the
list, return all of the numbers.
