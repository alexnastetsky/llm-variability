# rank_items

`def rank_items(items: list) -> list`

Given `items`, a list of `[name, score]` pairs (each name is a distinct string, each
score is a number), return the list of **names ranked from highest score to lowest**.

- The result is a list containing every name exactly once.
- Higher score comes earlier in the result.
