# parse_bool

`def parse_bool(s: str) -> bool`

Interpret the string `s` as a boolean and return `True` or `False`. The strings `"true"` and
`"false"` (case-insensitive, ignoring surrounding whitespace) must map to `True` and `False`
respectively.
