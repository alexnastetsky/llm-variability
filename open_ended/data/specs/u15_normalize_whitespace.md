# normalize_whitespace

`def normalize_whitespace(s: str) -> str`

**Normalize the whitespace** in `s`: return a string with the same words, in the same order, but
with the whitespace between them tidied up.
